import { TrafficRecord, PredictionResult, APIMonitorConfig } from '../types';
export declare class TrafficMonitor {
    private config;
    private trafficLog;
    constructor(config: APIMonitorConfig);
    record(record: TrafficRecord): void;
    predict(endpoint: string): Promise<PredictionResult>;
}
