import { RefactorSuggestion, RefactorConfig } from '../types';
export declare class RefactorEngine {
    private config;
    constructor(config: RefactorConfig);
    refactorCode(code: string): Promise<RefactorSuggestion[]>;
    refactorFile(filePath: string): Promise<RefactorSuggestion[]>;
}
